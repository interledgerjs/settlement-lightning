const lndServerPlugin = require('..')

/* Need to figure out how is best generate
 * wallet credentials for testing. */


async function createPlugin () {

}

beforeEach(async () => {

})

afterEach(async () => {

})

describe('ilp-plugin-lnd tests', () => {

})
